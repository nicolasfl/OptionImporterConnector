services:
    extensions_option_importer_connector.provider.form.job_instance:
        class: '%pim_enrich.provider.form.job_instance.class%'
        arguments:
            -
                csv_option_importer_import: pim-job-instance-option-importer-import
        tags:
            - { name: pim_enrich.provider.form }
